#===========================================================
# Questa DO File
#===========================================================

# Add all signals in the design
add wave -r sim:/counter_tb/*
# Run simulation until completion
run -all

exit
